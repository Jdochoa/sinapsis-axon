unit sinapsis.axn.m.repositorio.est;

interface
uses
  sinapsis.axn.m.repositorio,
  sinapsis.axn.m.repositorio.est.interfaz,
  sinapsis.axn.e.est
  ;
(*
type

  TEstRepository<TEntity: class, constructor; TKey> = class(TCtlRepository<TEntity, TKey>, IEstRepository<TEntity, TKey>)
  strict private
    { private declarations }
  strict protected
    { protected declarations }
  public
    { public declarations }
    function FindByCodigo(const ACodigo : string):TEEst0; virtual; abstract;

  end;
  *)
implementation

end.
